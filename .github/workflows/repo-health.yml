name: Repository He**th Check

on**  push:
    branches:
      - mai**
jobs**  health-check:
    runs-on: ubun**-latest

  name: Repository Health Check

on:
  push:
    branches:
      - main

jobs:
  health-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: List Files
        run: ls -R

      - name: List Files
      **run: ls -R

      -**ame**Check JSON Files
        run: |
 **       find . -name "*.json" -exe***ython -m json.tool {} \;
